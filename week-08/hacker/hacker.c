#include "hacker.h"
/*
 int binary_converter(char bin_nums[32])
    {
        int dec_num =0;
        int base = 1;
        int remainder = 0;
        int bin_num_int = bin_nums;

        while (bin_nums > 0)
        {
            remainder = bin_num_int % 10;
            dec_num = dec_num + remainder * base;
            bin_num_int = bin_num_int / 10;
            base = base * 2;
        }
        return printf("\n%d\n", dec_num);
    }
*/
